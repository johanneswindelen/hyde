<nav role="navigation">
    <ul class="navbar">
        {% for p in nav_bar_pages %}
        <li class="navbar"><a href="{{ p.url }}">{{ p.meta.title }}</a></li>
        {% endfor %}
        {% for p in indices %}
        <li class="navbar"><a href="{{ p.url }}">{{ p.meta.title }}</a></li>
        {% endfor %}
    </ul>
</nav>